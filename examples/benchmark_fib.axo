// Benchmark: Fibonacci (recursive)
// Compute fib(30) multiple times

func fib(n: int) -> int {
    if (n <= 1) {
        return n;
    }
    return fib(n - 1) + fib(n - 2);
}

var result: int = fib(25);
print("fib(25) =", result);
