// Test the compiler engine
func fibonacci(n: int) -> int {
    if (n <= 1) {
        return n;
    }
    return fibonacci(n - 1) + fibonacci(n - 2);
}

func main() -> void {
    var x: int = 5;
    var y: int = 10;
    var sum: int = x + y;
}
